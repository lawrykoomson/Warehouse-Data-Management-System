warehouse_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      user: postgres
      password: "{{ env_var('DBT_POSTGRES_PASSWORD') }}"
      dbname: warehouse_db
      schema: warehouse_dw
      threads: 4
      connect_timeout: 10